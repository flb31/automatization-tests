name: Slack Alert

on:
  workflow_call:
    inputs:
      channel_id:
        description: "Slack channel ID"
        required: true
        type: string
      reason:
        description: "Reason for the alert"
        required: true
        type: string
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Sends echo
        run: |
          echo "Send notification failure ${{ secrets.SLACK_BOT_TOKEN }} and ${{ inputs.channel_id }}"
      - name: Send alert to Slack
        uses: slackapi/slack-github-action@v2.1.1
        with:
          method: chat.postMessage
          token: xoxb-9238536105170-9236661944917-q0xCLcKCTIxFWGnI4BA7S1ka
          payload: |
            channel: ${{ inputs.channel_id }}
            blocks:
              - type: "section"
                text:
                  type: "mrkdwn"
                  text: "🚨  *${{ inputs.reason }}*"

